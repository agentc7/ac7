<!--
  Thanks for contributing to AgentC7!

  Before submitting:
  - Read CONTRIBUTING.md for the dev loop and commit conventions.
  - Sign off every commit: `git commit -s` (see the DCO section).
  - Make sure `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass locally.
-->

## Summary

<!-- 1-3 sentences. What does this change, and why? -->

## What changed

<!-- Bulleted list of concrete changes. Skip this if the summary covers it. -->

-
-

## Test plan

<!-- How you verified this. New tests? Manual repro? Include commands. -->

- [ ]
- [ ]

## Related

<!-- Issue / discussion links. Use `Closes #123` to auto-close on merge. -->

## Checklist

- [ ] All commits are signed off (`git commit -s`) — the DCO bot will confirm
- [ ] `pnpm lint` is clean
- [ ] `pnpm typecheck` is clean
- [ ] `pnpm test` passes
- [ ] Docs/README updated if user-facing behavior changed
- [ ] I've added myself to `AUTHORS` if this is my first contribution
